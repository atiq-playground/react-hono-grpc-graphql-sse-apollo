version: v2
modules:
  - path: proto
lint:
  use:
    - STANDARD
  except:
    # IngestBlocks streams FindingBlock directly to keep the bounded columnar
    # message as the server-to-server ingestion unit.
    - RPC_REQUEST_STANDARD_NAME
breaking:
  use:
    - FILE
