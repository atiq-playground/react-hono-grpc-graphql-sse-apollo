version: v2
modules:
  - path: proto
lint:
  use:
    - STANDARD
  except:
    # Ticket T03 freezes FindingBlock / StreamRequest names for all consumers.
    - RPC_REQUEST_STANDARD_NAME
    - RPC_RESPONSE_STANDARD_NAME
breaking:
  use:
    - FILE
